{
  "name": "full-stack-story",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "pnpm install",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build"
  }
}